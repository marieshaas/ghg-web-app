# URL backend API (tanpa trailing slash, tanpa /api)
# Contoh jika backend di-deploy ke Railway:
# REACT_APP_API_URL=https://ghg-backend.up.railway.app
#
# Kalau dikosongkan / tidak di-set, otomatis fallback ke http://localhost:3001
REACT_APP_API_URL=
